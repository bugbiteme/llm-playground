{
  "name": "llm-chat-client",
  "version": "0.1.0",
  "description": "A web chat client for custom LLM endpoints with dynamic headers and model tuning.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": "^18 || ^20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.4"
  }
}
